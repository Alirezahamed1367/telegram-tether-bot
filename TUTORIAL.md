# 📚 آموزش کامل گام‌به‌گام راه‌اندازی ربات (برای مبتدی‌ها)

این راهنما شما را **از صفر** و با زبان **خیلی ساده** در راه‌اندازی ربات همراهی می‌کند.

⚠️ **نکته مهم:** شما نمی‌توانید ربات را به کانال `@tetherprice_toman` اضافه کنید چون ادمین آن نیستید.  
✅ **راه‌حل:** یک کانال میانی می‌سازیم که ربات بتواند از آن بخواند.

**زمان کل:** حدود ۴۵ دقیقه

---

## 🎯 قدم ۱: ساخت ربات تلگرام (۵ دقیقه)

### 1.1 - باز کردن BotFather

1. تلگرام را باز کنید
2. در قسمت جستجو `@BotFather` را تایپ کنید
3. روی BotFather کلیک کنید و `/start` را بزنید

### 1.2 - ساخت ربات جدید

1. دستور `/newbot` را ارسال کنید
2. یک **نام** برای ربات انتخاب کنید (مثلاً: `Yuan Rate Calculator`)
3. یک **username** انتخاب کنید که باید به `bot` ختم شود (مثلاً: `yuan_rate_calculator_bot`)

### 1.3 - دریافت توکن

BotFather یک پیام مثل این می‌فرستد:

```
Done! Congratulations on your new bot. You will find it at 
t.me/yuan_rate_calculator_bot. You can now add a description...

Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789

For a description of the Bot API, see this page: 
https://core.telegram.org/bots/api
```

⚠️ **مهم:** توکن را کپی کنید و در جای امنی ذخیره کنید (هرگز آن را با دیگران به اشتراک نگذارید)

---

## 🎯 قدم ۲: ساخت کانال میانی (۵ دقیقه) - **مهم!**

چون شما ادمین کانال `@tetherprice_toman` نیستید و نمی‌توانید ربات را به آن اضافه کنید، باید یک کانال میانی بسازید.

### 2.1 - ساخت کانال خصوصی

1. در تلگرام، روی منوی اصلی کلیک کنید (سه خط کنار نام شما)
2. **New Channel** (کانال جدید) را انتخاب کنید
3. نام کانال را مثلاً `Tether Price Cache` بگذارید
4. توضیحات دلخواه بنویسید (یا خالی بگذارید)
5. روی **Next** کلیک کنید
6. **Private Channel** (کانال خصوصی) را انتخاب کنید ⚠️
7. روی **Create** کلیک کنید

### 2.2 - اضافه کردن ربات به کانال

1. در کانال جدید، روی نام کانال (بالای صفحه) کلیک کنید
2. **Administrators** (مدیران) را انتخاب کنید
3. **Add Administrator** را بزنید
4. ربات خود را جستجو کنید (username ربات که ساختید)
5. ربات را انتخاب کنید
6. **تمام دسترسی‌ها** را فعال کنید (یا حداقل: Post Messages)
7. **Save** کنید

✅ حالا ربات شما ادمین این کانال است و می‌تواند از آن بخواند!

### 2.3 - دریافت شناسه کانال میانی

**روش ۱: استفاده از ربات خودتان**

1. به گفتگوی خصوصی با ربات خود بروید
2. به کانال میانی بروید و یک پیام تستی بفرستید (مثلاً: `test`)
3. این کد پایتون را اجرا کنید تا شناسه کانال را پیدا کنید:

```powershell
# در PowerShell اجرا کنید:
python -c "
import asyncio
from telegram import Bot

async def get_updates():
    bot = Bot('YOUR_BOT_TOKEN_HERE')  # توکن ربات خود را جایگزین کنید
    updates = await bot.get_updates()
    for update in updates:
        if update.channel_post:
            print(f'Channel ID: {update.channel_post.chat.id}')
            print(f'Channel Name: {update.channel_post.chat.title}')

asyncio.run(get_updates())
"
```

شناسه کانال را کپی کنید (مثل: `-1001234567890`)

**روش ۲: استفاده از userinfobot (ساده‌تر)**

1. به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. یک پیام از کانال میانی خود را به این ربات **forward** کنید
3. ربات شناسه کانال را می‌دهد (مثلاً: `-1001234567890`)

⚠️ **این شناسه را یادداشت کنید - بعداً لازم است!**

---

## 🎯 قدم ۳: آماده‌سازی گروه تلگرام (۳ دقیقه)

### 3.1 - ایجاد گروه

1. در تلگرام یک **گروه جدید** بسازید
2. نام آن را مثلاً `Yuan Rate Updates` بگذارید
3. ربات خود را به گروه **اضافه** کنید

### 3.2 - دریافت شناسه گروه

**روش ۱: استفاده از userinfobot**

1. به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. یک پیام از گروه خود را به این ربات **forward** کنید
3. ربات شناسه گروه را می‌دهد (مثلاً: `-1001234567890`)

**روش ۲: استفاده از API تلگرام**

1. ربات را به گروه اضافه کنید
2. در گروه یک پیام بفرستید (مثلاً: `سلام`)
3. در مرورگر این آدرس را باز کنید:

```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```

(جای `<YOUR_BOT_TOKEN>` توکن ربات خود را بگذارید)

4. در JSON نتیجه، به دنبال `"chat":{"id":-1001234567890` بگردید

⚠️ **توجه:** شناسه گروه‌ها با منفی (`-`) شروع می‌شود

---

## 🎯 قدم ۴: ساخت حساب GitHub (۵ دقیقه)

اگر حساب GitHub ندارید:

1. به [github.com](https://github.com) بروید
2. روی **Sign up** کلیک کنید
3. ایمیل، رمز عبور و نام کاربری خود را وارد کنید
4. ایمیل خود را تایید کنید

---

## 🎯 قدم ۵: آپلود پروژه به GitHub (۱۰ دقیقه)

### 5.1 - ایجاد ریپازیتوری جدید

1. وارد GitHub شوید
2. روی دکمه **+** (بالا سمت راست) کلیک کنید
3. **New repository** را انتخاب کنید
4. نام ریپازیتوری را `telegram-tether-bot` بگذارید
5. **Public** یا **Private** را انتخاب کنید (هر دو کار می‌کند)
6. روی **Create repository** کلیک کنید

### 5.2 - آپلود فایل‌ها

**روش ۱: از طریق وب (ساده‌تر برای مبتدی‌ها) ⭐**

1. در صفحه ریپازیتوری خود، روی **uploading an existing file** کلیک کنید
2. تمام فایل‌های پروژه را انتخاب کنید:
   - `bot.py`
   - `requirements.txt`
   - `.gitignore`
   - `.env.example`
   - `README.md`
   - همه فایل‌های `.md`
   - پوشه `.github` با تمام محتویاتش
3. در قسمت commit، پیامی مثل `Initial commit` بنویسید
4. روی **Commit changes** کلیک کنید

⚠️ **نکته:** اگر پوشه `.github` آپلود نشد، باید دستی آن را ایجاد کنید:
- در ریپازیتوری: **Add file** > **Create new file**
- نام فایل: `.github/workflows/update_rate.yml`
- محتوای فایل `update_rate.yml` را کپی-پیست کنید
- **Commit** کنید

**روش ۲: از طریق Git (اگر با Git آشنا هستید)**

```powershell
cd "d:\Project\Telegram Tether"
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/telegram-tether-bot.git
git branch -M main
git push -u origin main
```

---

## 🎯 قدم ۶: تنظیم Secrets در GitHub (۵ دقیقه) - **بسیار مهم!**

### 6.1 - رفتن به تنظیمات

1. در ریپازیتوری GitHub خود، به تب **Settings** بروید
2. از منوی سمت چپ، **Secrets and variables** > **Actions** را انتخاب کنید

### 6.2 - اضافه کردن Secret اول: توکن ربات

1. روی **New repository secret** کلیک کنید
2. **Name:** بنویسید `BOT_TOKEN` (دقیقاً همین - با حروف بزرگ)
3. **Secret:** توکن ربات را paste کنید (که از BotFather گرفتید)
4. روی **Add secret** کلیک کنید

### 6.3 - اضافه کردن Secret دوم: شناسه گروه

1. دوباره روی **New repository secret** کلیک کنید
2. **Name:** بنویسید `TARGET_GROUP_ID` (دقیقاً همین - با حروف بزرگ)
3. **Secret:** شناسه گروه را paste کنید (با منفی، مثل: `-1001234567890`)
4. روی **Add secret** کلیک کنید

### 6.4 - اضافه کردن Secret سوم: شناسه کانال میانی ⭐

1. دوباره روی **New repository secret** کلیک کنید
2. **Name:** بنویسید `PRIVATE_CHANNEL_ID` (دقیقاً همین - با حروف بزرگ)
3. **Secret:** شناسه کانال میانی را paste کنید (که در قدم ۲ گرفتید، مثل: `-1001234567890`)
4. روی **Add secret** کلیک کنید

✅ حالا باید **سه Secret** داشته باشید:
- `BOT_TOKEN` (توکن ربات)
- `TARGET_GROUP_ID` (شناسه گروه که پیام‌ها را می‌فرستد)
- `PRIVATE_CHANNEL_ID` (شناسه کانال میانی که ربات از آن می‌خواند)

---

## 🎯 قدم ۷: فعال‌سازی GitHub Actions (۲ دقیقه)

1. در ریپازیتوری خود به تب **Actions** بروید
2. اگر پیام "Workflows disabled" دیدید:
   - روی **I understand my workflows, go ahead and enable them** کلیک کنید
3. باید workflow به نام **Update Tether Rate** را ببینید

---

## 🎯 قدm ۸: تنظیم نرخ یوآن و تست (۵ دقیقه)

### 8.1 - تنظیم نرخ یوآن

1. در تلگرام به گفتگوی خصوصی با ربات خود بروید
2. دستور `/start` را بفرستید
3. دستور زیر را بفرستید (نرخ روز خود را وارد کنید):

```
/setrate 7.12
```

ربات پیامی مثل این می‌فرستد:

```
✅ نرخ یوآن به 7.12 تنظیم شد.
🕐 زمان: 2025/11/10 - 10:30
```

### 8.2 - بررسی وضعیت

دستور `/status` را بفرستید تا ببینید همه چیز درست است:

```
📊 وضعیت ربات:

💱 نرخ یوآن: 7.12
📈 آخرین نرخ محاسبه شده: ❌ محاسبه نشده
📢 کانال منبع: @tetherprice_toman
🎯 گروه مقصد: -1001234567890
🕐 زمان فعلی: 2025/11/10 - 10:31:25
```

---

## 🎯 قدم ۹: Forward کردن پیام از کانال اصلی به کانال میانی (۲ دقیقه) - **مهمترین قدم!**

این کار را **هر روز** یا **هر ساعت** (وقتی نرخ جدید آمد) باید انجام دهید:

### 9.1 - Forward کردن پیام

1. به کانال `@tetherprice_toman` بروید
2. **آخرین پیامی** که نرخ تتر را دارد پیدا کنید (مثل این):

```
💵 قیمت لحظه‌ای تتر

🟢 خرید تتر : 1084970 ریال
🔴 فروش تتر : 1084980 ریال

🥇 طلای 18 عیار : 104721000 ریال
🟡 سکه بهار آزادی : 1040850000 ریال

@tetherprice_toman
```

3. روی پیام **کلیک راست** کنید (یا روی دکمه Forward)
4. **Forward** را بزنید
5. کانال میانی خود را انتخاب کنید (Tether Price Cache)
6. ارسال کنید

✅ حالا پیام در کانال میانی شماست و ربات می‌تواند آن را بخواند!

### 9.2 - چرا این کار لازم است؟

- ربات نمی‌تواند از `@tetherprice_toman` بخواند چون شما ادمین آن نیستید
- کانال میانی یک "کپی" از پیام‌ها را نگه می‌دارد
- ربات از کانال میانی می‌خواند (چون ادمین آن است)

⚠️ **یادتان باشد:** هر بار که نرخ جدید آمد، باید این Forward را انجام دهید!

---

## 🎯 قدم ۱۰: تست نهایی - اجرای ربات (۵ دقیقه)

حالا همه چیز آماده است! بیایید تست کنیم:

### 10.1 - تست دستی از طریق GitHub Actions

1. در ریپازیتوری GitHub خود، به تب **Actions** بروید
2. از سمت چپ، **Update Tether Rate** را انتخاب کنید
3. روی دکمه **Run workflow** (سمت راست بالا) کلیک کنید
4. روی دکمه سبز **Run workflow** کلیک کنید
5. صبر کنید تا workflow اجرا شود (حدود ۳۰ ثانیه)

### 10.2 - بررسی لاگ‌ها

1. workflow شروع به اجرا می‌کند (نقطه زرد 🟡)
2. بعد از اتمام (تیک سبز ✅ یا ضربدر قرمز ❌) روی آن کلیک کنید
3. روی **update-rate** کلیک کنید
4. روی **Run bot update** کلیک کنید تا لاگ‌ها را ببینید

### 10.3 - اگر موفق بود:

باید در گروه تلگرامی خود پیامی مثل این ببینید:

```
⏳ به‌روزرسانی نرخ :
1️⃣ خرید تا 5 هزار یوآن : 15,300
2️⃣ خرید تا 10 هزار یوآن : 15,290
3️⃣ خرید بالای 10 هزار یوآن : 15,280

📊 نرخ مبنا: 15,240 تومان
🕐 زمان به‌روزرسانی: 2025/11/10 - 14:00
💱 نرخ یوآن: 7.12
```

✅ **تبریک! ربات شما کار می‌کند!** 🎉

### 10.4 - اگر خطا داد:

به بخش [عیب‌یابی](#عیب-یابی) مراجعه کنید.

---

## 🔄 استفاده روزانه (بعد از راه‌اندازی)

### هر روز قبل از ساعت ۱۱ صبح:

**کار ۱: تنظیم نرخ یوآن**
```
به ربات پیام دهید:
/setrate 7.15
```

**کار ۲: Forward کردن پیام**
1. به کانال `@tetherprice_toman` بروید
2. آخرین پیام را Forward کنید به کانال میانی خود

### بعد از آن:

✅ ربات **خودکار** هر ساعت (از ۱۱ صبح تا ۷ شب) کار می‌کند!  
✅ پیام‌ها را در گروه دریافت می‌کنید!

---
```

خروجی باید چیزی شبیه این باشد:

```
🧪 شروع تست‌های ربات...

1️⃣ تست استخراج قیمت از متن کانال
✅ قیمت استخراج شده: 1,084,980 ریال

2️⃣ تست محاسبه نرخ مبنا
✅ نرخ مبنا محاسبه شده: 15,240 تومان

3️⃣ تست قالب‌بندی پیام نهایی

📨 پیام نهایی:
==================================================
⏳ به‌روزرسانی نرخ :
1️⃣ خرید تا 5 هزار یوآن : 15,300
2️⃣ خرید تا 10 هزار یوآن : 15,290
3️⃣ خرید بالای 10 هزار یوآن : 15,280
...
==================================================

✅ همه تست‌ها با موفقیت انجام شد!
```

---

## 🎯 قدم ۸: تست GitHub Actions (۵ دقیقه)

### 8.1 - اجرای دستی Workflow

1. در GitHub به تب **Actions** بروید
2. از سمت چپ، **Update Tether Rate** را انتخاب کنید
3. روی **Run workflow** (سمت راست) کلیک کنید
4. روی دکمه سبز **Run workflow** کلیک کنید

### 8.2 - بررسی لاگ‌ها

1. workflow شروع به اجرا می‌کند (نقطه زرد)
2. بعد از اتمام (تیک سبز یا ضربدر قرمز) روی آن کلیک کنید
3. روی **update-rate** کلیک کنید
4. لاگ‌ها را بررسی کنید

اگر خطایی ببینید، به بخش [عیب‌یابی](#عیب-یابی) مراجعه کنید.

---

## 🎯 قدم ۹: حل مشکل خواندن کانال (مهم!)

⚠️ **مشکل اصلی:** ربات‌های تلگرام نمی‌توانند مستقیماً از کانال‌های عمومی بخوانند.

### راه‌حل پیشنهادی: ایجاد کانال میانی

1. یک **کانال خصوصی** جدید بسازید (مثلاً: `My Tether Cache`)
2. ربات را به این کانال اضافه کنید و **ادمین** کنید
3. هر روز **دستی** آخرین پیام از `@tetherprice_toman` را به این کانال forward کنید

سپس در فایل `.env` این مورد را اضافه کنید:

```env
PRIVATE_CHANNEL_ID=-1001234567890  # شناسه کانال خصوصی شما
```

**یا**

از راهنمای پیشرفته (`ADVANCED.md`) برای استفاده از Telethon استفاده کنید.

---

## 🎯 قدم ۱۰: استفاده روزانه

### هر روز قبل از ساعت ۱۱:

1. به ربات پیام بدهید: `/setrate 7.15` (نرخ روز)
2. اگر از کانال میانی استفاده می‌کنید، پیام جدید را forward کنید

### ربات به صورت خودکار:

- هر ساعت از ۱۱ صبح تا ۷ شب اجرا می‌شود
- نرخ را محاسبه و به گروه ارسال می‌کند
- اگر نرخ پایین بیاید، از نرخ قبلی استفاده می‌کند

---

## 🐛 عیب‌یابی (اگر مشکلی پیش آمد)

### ❌ مشکل: ربات پیام ارسال نمی‌کند

**چک‌لیست:**
- [ ] ربات عضو گروه است؟
- [ ] ربات ادمین کانال میانی است؟
- [ ] نرخ یوآن با `/setrate` تنظیم شده؟
- [ ] پیام از `@tetherprice_toman` به کانال میانی forward شده؟
- [ ] سه Secret در GitHub تنظیم شده؟ (BOT_TOKEN, TARGET_GROUP_ID, PRIVATE_CHANNEL_ID)

**راه‌حل:**
1. دستور `/status` را در تلگرام به ربات بدهید
2. در GitHub Actions لاگ‌ها را بررسی کنید
3. مطمئن شوید همه قدم‌ها انجام شده

---

### ❌ مشکل: خطای "BOT_TOKEN not set"

**دلیل:** Secret در GitHub تنظیم نشده

**راه‌حل:**
1. GitHub > Settings > Secrets > Actions
2. مطمئن شوید این سه Secret موجود است:
   - `BOT_TOKEN`
   - `TARGET_GROUP_ID`
   - `PRIVATE_CHANNEL_ID`
3. دقیقاً همین نام‌ها (با حروف بزرگ) استفاده کنید

---

### ❌ مشکل: "قیمت تتر در پیام کانال یافت نشد"

**دلیل:** یا پیام forward نشده، یا فرمت متن فرق کرده

**راه‌حل:**
1. مطمئن شوید پیام را از `@tetherprice_toman` به کانال میانی forward کردید
2. بررسی کنید پیام حاوی عبارت "فروش تتر" و عدد قیمت است
3. اگر فرمت کانال عوض شده، در گروه Issues گیت‌هاب اطلاع دهید

---

### ❌ مشکل: GitHub Actions اجرا نمی‌شود

**راه‌حل:**
1. تب Actions > مطمئن شوید workflows فعال است
2. اگر "Workflows disabled" دیدید، "Enable workflows" را بزنید
3. Cron schedule ممکن است ۵-۱۰ دقیقه تاخیر داشته باشد
4. می‌توانید دستی اجرا کنید: Actions > Update Tether Rate > Run workflow

---

### ❌ مشکل: ربات نرخ یوآن را قبول نمی‌کند

**دلیل:** فرمت اشتباه

**راه‌حل:**
- ✅ درست: `/setrate 7.12` (با نقطه)
- ❌ اشتباه: `/setrate 7,12` (با کاما)
- ❌ اشتباه: `/setrate7.12` (بدون فاصله)

---

### ❌ مشکل: پیام‌ها تکراری ارسال می‌شود

**دلیل:** ممکن است چند بار پیام را forward کرده باشید

**راه‌حل:**
- کانال میانی را پاک کنید و فقط آخرین پیام را forward کنید
- یا صبر کنید تا ساعت بعد

---

### 💡 تست محلی (اگر مشکل پیدا نکردید)

برای تست در کامپیوتر خودتان:

```powershell
# نصب وابستگی‌ها
cd "d:\Project\Telegram Tether"
pip install -r requirements.txt

# تست محاسبات (بدون نیاز به تلگرام)
python test_bot.py

# اجرای ربات به صورت محلی
# اول فایل .env بسازید و توکن‌ها را بگذارید
Copy-Item .env.example .env
notepad .env  # ویرایش کنید

python bot.py
```

---

## 🎉 تبریک! ربات شما آماده است!

### ✅ چک‌لیست نهایی:

- [ ] ربات تلگرام ساخته شد ✓
- [ ] کانال میانی خصوصی ساخته شد ✓
- [ ] ربات ادمین کانال میانی است ✓
- [ ] گروه تلگرام ساخته شد ✓
- [ ] ربات به گروه اضافه شد ✓
- [ ] پروژه در GitHub آپلود شد ✓
- [ ] سه Secret تنظیم شد ✓
- [ ] GitHub Actions فعال شد ✓
- [ ] نرخ یوآن تنظیم شد ✓
- [ ] پیام از کانال اصلی forward شد ✓
- [ ] ربات تست شد و کار کرد ✓

---

## 📅 استفاده روزانه (خیلی ساده!)

### صبح هر روز (قبل از ۱۱):

1️⃣ به ربات پیام دهید:
```
/setrate 7.15
```
(نرخ یوآن روز را بگذارید)

2️⃣ به کانال `@tetherprice_toman` بروید، آخرین پیام را به کانال میانی خود forward کنید

### تمام! ✅

ربات بقیه کار را خودکار انجام می‌دهد:
- هر ساعت از ۱۱ صبح تا ۷ شب اجرا می‌شود
- نرخ را محاسبه می‌کند
- پیام را در گروه ارسال می‌کند

---

## 📞 نیاز به کمک بیشتر؟

1. **راهنمای کامل:** `README.md`
2. **تنظیمات پیشرفته:** `ADVANCED.md`
3. **راهنمای سریع:** `QUICKSTART.md`
4. **سوالات:** Issues در GitHub بپرسید

---

## 🎁 نکته‌های اضافی

### 💡 می‌خواهید اتوماتیک‌تر شود؟

اگر نمی‌خواهید هر روز دستی forward کنید، می‌توانید:
- از `Telethon` استفاده کنید (راهنما در `ADVANCED.md`)
- یک ربات جداگانه برای auto-forward بسازید
- یک اسکریپت پایتون در کامپیوتر خود اجرا کنید

### 🔔 اعلان‌ها

می‌توانید ربات را طوری تنظیم کنید که:
- وقتی نرخ تغییر کرد به شما پیام بدهد
- گزارش روزانه بفرستد
- خطاها را به شما اطلاع دهد

### 📊 آمار

در فایل `data.json` (در ریپازیتوری GitHub) می‌توانید:
- تاریخچه نرخ‌ها را ببینید
- آخرین به‌روزرسانی را چک کنید

---

**موفق و پرسود باشید! 🚀💰**
