# 🤖 ربات تلگرام محاسبه نرخ یوآن

این ربات به صورت **کاملاً خودکار** هر ساعت از **11 صبح تا 7 شب** نرخ فروش تتر را از کانال [@tetherprice_toman](https://t.me/tetherprice_toman) دریافت کرده و با استفاده از نرخ یوآن، قیمت نهایی را محاسبه و در گروه تلگرامی ارسال می‌کند.

## 🚀 دو روش راه‌اندازی

### 🔥 روش ۱: کاملاً خودکار (پیشنهادی) ⭐
- ✅ **بدون نیاز به forward دستی** از کانال
- ✅ **بدون نیاز به سیستم روشن**
- ✅ استفاده از Telethon برای خواندن مستقیم کانال
- 👉 [راهنمای کامل خودکار](AUTO_SETUP.md)
- 👉 [راهنمای سریع](QUICK_AUTO.md)

### 📌 روش ۲: نیمه‌خودکار
- ⚠️ نیاز به forward دستی پیام از کانال
- ✅ بدون نیاز به سیستم روشن
- 👉 [راهنمای قدم‌به‌قدم](TUTORIAL.md)
- 👉 [راهنمای ساده](SIMPLE_GUIDE.md)

---

## 📋 فهرست مطالب

- [ویژگی‌ها](#ویژگیها)
- [پیش‌نیازها](#پیشنیازها)
- [نصب سریع](#نصب-سریع)
- [استفاده](#استفاده)
- [نحوه کار ربات](#نحوه-کار-ربات)
- [عیب‌یابی](#عیبیابی)

---

## ✨ ویژگی‌ها

✅ **اجرای خودکار بدون نیاز به سرور** - استفاده از GitHub Actions  
✅ **خواندن خودکار از کانال عمومی** - با Telethon (بدون forward دستی)  
✅ **محاسبه هوشمند نرخ مبنا** با رند کردن به بالا  
✅ **شرط نرخ کاهشی** - اگر نرخ جدید کمتر باشد، از نرخ قبلی استفاده می‌شود  
✅ **ذخیره خودکار داده‌ها** در GitHub  
✅ **کنترل دستی** از طریق دستورات تلگرام  
✅ **کاملاً رایگان** - بدون نیاز به سرور VPS  
✅ **منطقه زمانی تهران**  

---

## 🔧 پیش‌نیازها

1. **حساب تلگرام**
2. **حساب GitHub** (رایگان)
3. **BotFather تلگرام** برای ساخت ربات
4. **API Credentials از my.telegram.org** (برای روش خودکار)

---

## ⚡ نصب سریع

### برای روش خودکار (پیشنهادی):

```powershell
# 1. کلون پروژه
git clone https://github.com/YOUR_USERNAME/telegram-tether-bot.git
cd telegram-tether-bot

# 2. نصب وابستگی‌ها
pip install -r requirements.txt

# 3. تنظیمات
cp .env.example .env
# ویرایش .env و پر کردن اطلاعات

# 4. تنظیم نرخ و تست
python bot.py
# در تلگرام: /setrate 7.12

python auto_fetcher.py
# کد تایید را وارد کنید
```

👉 **راهنمای کامل:** [AUTO_SETUP.md](AUTO_SETUP.md)

---

## 📦 نصب و راه‌اندازی (جزئیات)

### مرحله ۱: ساخت ربات تلگرام

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نام و username برای ربات انتخاب کنید
4. **توکن ربات** را کپی کنید (مثل: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### مرحله ۲: دریافت API Credentials (برای روش خودکار)

1. به [https://my.telegram.org](https://my.telegram.org) بروید
2. با شماره تلفن login کنید
3. API development tools > Create application
4. **API ID** و **API Hash** را کپی کنید

### مرحله ۳: دریافت شناسه گروه

1. ربات را به گروه تلگرامی خود اضافه کنید
2. به [@userinfobot](https://t.me/userinfobot) پیام دهید
3. گروه را به این ربات forward کنید تا شناسه را دریافت کنید (مثل: `-1001234567890`)

یا از این روش استفاده کنید:

```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```

(توکن ربات خود را جایگزین کنید)

### مرحله ۳: ایجاد ریپازیتوری GitHub

1. وارد [GitHub](https://github.com) شوید
2. یک ریپازیتوری **جدید** ایجاد کنید (می‌توانید نام `telegram-tether-bot` بگذارید)
3. ریپازیتوری را **Public** یا **Private** بسازید (هر دو کار می‌کند)

### مرحله ۴: آپلود کردن فایل‌ها به GitHub

#### روش ۱: از طریق Git (توصیه می‌شود)

```powershell
# وارد پوشه پروژه شوید
cd "d:\Project\Telegram Tether"

# Git را راه‌اندازی کنید
git init
git add .
git commit -m "Initial commit"

# به ریپازیتوری GitHub متصل شوید (آدرس خود را جایگزین کنید)
git remote add origin https://github.com/YOUR_USERNAME/telegram-tether-bot.git
git branch -M main
git push -u origin main
```

#### روش ۲: از طریق وب GitHub

1. وارد ریپازیتوری خود در GitHub شوید
2. روی **Add file** > **Upload files** کلیک کنید
3. تمام فایل‌های پروژه را آپلود کنید

### مرحله ۵: تنظیم Secrets در GitHub

1. در ریپازیتوری GitHub خود، به **Settings** > **Secrets and variables** > **Actions** بروید
2. روی **New repository secret** کلیک کنید
3. دو Secret زیر را اضافه کنید:

| نام              | مقدار                                                             |
| ------------------- | ---------------------------------------------------------------------- |
| `BOT_TOKEN`       | توکن ربات تلگرام شما                                  |
| `TARGET_GROUP_ID` | شناسه گروه تلگرام (با منفی شروع می‌شود) |

### مرحله ۶: فعال‌سازی GitHub Actions

1. در ریپازیتوری خود به تب **Actions** بروید
2. اگر پیغام "Workflows disabled" دیدید، روی **Enable workflows** کلیک کنید
3. Workflow به نام **Update Tether Rate** را خواهید دید

---

## 🎮 استفاده

### تنظیم نرخ یوآن

قبل از ساعت 11 صبح، نرخ یوآن روز را به ربات بدهید:

```
/setrate 7.12
```

### دستورات دیگر

| دستور            | توضیحات                                          |
| --------------------- | ------------------------------------------------------- |
| `/start`            | شروع و نمایش راهنما                     |
| `/setrate <نرخ>` | تنظیم نرخ یوآن (مثال:`/setrate 7.12`) |
| `/getrate`          | نمایش نرخ فعلی یوآن                     |
| `/status`           | نمایش وضعیت ربات                          |
| `/update`           | به‌روزرسانی دستی نرخ                  |

### اجرای محلی (اختیاری)

اگر می‌خواهید ربات را روی سیستم خود اجرا کنید:

```powershell
# نصب وابستگی‌ها
pip install -r requirements.txt

# ایجاد فایل .env
Copy-Item .env.example .env

# ویرایش .env و اضافه کردن توکن و شناسه گروه
notepad .env

# اجرای ربات
python bot.py
```

---

## 🔄 نحوه کار ربات

### فرآیند خودکار (هر ساعت از 11 صبح تا 7 شب):

```
1️⃣ دریافت آخرین پیام از کانال @tetherprice_toman
    ↓
2️⃣ استخراج قیمت فروش تتر (مثلاً: 1,084,980 ریال)
    ↓
3️⃣ تبدیل به تومان: 1,084,980 ÷ 10 = 108,498 تومان
    ↓
4️⃣ تقسیم بر نرخ یوآن: 108,498 ÷ 7.12 = 15,238.48
    ↓
5️⃣ رند کردن به بالا (نزدیکترین 10): 15,240 تومان
    ↓
6️⃣ بررسی شرط: اگر نرخ جدید < نرخ قبلی → از نرخ قبلی استفاده شود
    ↓
7️⃣ محاسبه قیمت‌های نهایی:
    • خرید تا 5 هزار: 15,240 + 60 = 15,300
    • خرید تا 10 هزار: 15,240 + 50 = 15,290
    • خرید بالای 10 هزار: 15,240 + 40 = 15,280
    ↓
8️⃣ ارسال پیام به گروه تلگرام
```

### نمونه پیام خروجی:

```
⏳ به‌روزرسانی نرخ :
1️⃣ خرید تا 5 هزار یوآن : 15,300
2️⃣ خرید تا 10 هزار یوآن : 15,290
3️⃣ خرید بالای 10 هزار یوآن : 15,280

📊 نرخ مبنا: 15,240 تومان
🕐 زمان به‌روزرسانی: 2025/11/10 - 14:00
💱 نرخ یوآن: 7.12
```

---

## 🐛 عیب‌یابی

### ربات پیام ارسال نمی‌کند

**راه‌حل:**

- مطمئن شوید ربات عضو گروه است
- بررسی کنید `BOT_TOKEN` و `TARGET_GROUP_ID` صحیح باشند
- در GitHub Actions لاگ‌ها را بررسی کنید

### نرخ یوآن تنظیم نمی‌شود

**راه‌حل:**

- از دستور `/setrate 7.12` استفاده کنید (نه `/setrate 7,12`)
- مطمئن شوید عدد مثبت است

### خطای "قیمت تتر در پیام کانال یافت نشد"

**راه‌حل:**
این خطا به این دلیل است که ربات نمی‌تواند مستقیماً از کانال عمومی بخواند.

**راه‌حل موقت:**
ربات را به کانال `@tetherprice_toman` اضافه کنید (به عنوان عضو).

**یا**

یک اسکریپت جداگانه برای خواندن کانال عمومی از طریق Telethon ایجاد کنید.

### GitHub Actions اجرا نمی‌شود

**راه‌حل:**

- به تب Actions بروید و مطمئن شوید فعال است
- زمان Cron را بررسی کنید (ممکن است با تاخیر اجرا شود)
- می‌توانید دستی اجرا کنید: Actions > Update Tether Rate > Run workflow

---

## 📝 نکات مهم

1. **نرخ یوآن را هر روز قبل از ساعت 11 تنظیم کنید**
2. **GitHub Actions ممکن است 5-10 دقیقه تاخیر داشته باشد**
3. **داده‌ها به صورت خودکار در `data.json` ذخیره می‌شوند**
4. **اگر نرخ کاهشی بود، نرخ قبلی حفظ می‌شود**

---

## 🚀 توسعه‌های آینده

- [ ] اضافه کردن قابلیت ارسال به واتس‌اپ
- [ ] دریافت خودکار نرخ یوآن از API
- [ ] پشتیبانی از چندین گروه
- [ ] گزارش روزانه
- [ ] نمودار تغییرات نرخ

---

## 📞 پشتیبانی

اگر مشکلی داشتید:

1. لاگ‌های GitHub Actions را بررسی کنید
2. از دستور `/status` برای بررسی وضعیت استفاده کنید
3. فایل `data.json` را در ریپازیتوری بررسی کنید

---

## 📜 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

**ساخته شده با ❤️ برای خرید و فروش یوآن**
