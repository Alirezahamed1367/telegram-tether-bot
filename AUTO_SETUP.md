# 🚀 راهنمای راه‌اندازی کاملاً خودکار

## 🎯 هدف: ربات ۱۰۰٪ خودکار بدون نیاز به حضور شما

این راهنما نحوه راه‌اندازی ربات را طوری توضیح می‌دهد که:
- ✅ **خودکار** از کانال `@tetherprice_toman` بخواند (بدون نیاز به forward دستی)
- ✅ **بدون نیاز به سرور** یا سیستم همیشه روشن
- ✅ **کاملاً رایگان** با استفاده از GitHub Actions
- ✅ هر ساعت از ۱۱ صبح تا ۷ شب خودکار اجرا می‌شود

---

## 📋 پیش‌نیازها

- حساب تلگرام (روی گوشی/کامپیوتر)
- حساب GitHub (رایگان)
- ۴۵ دقیقه وقت برای راه‌اندازی اولیه

---

## 🔑 قدم ۱: دریافت API Credentials از تلگرام (۱۰ دقیقه) ⭐

این مهمترین قدم است! با این اطلاعات می‌توانید با حساب کاربری خود به تلگرام وصل شوید.

### 1.1 - رفتن به سایت رسمی تلگرام

1. به [https://my.telegram.org](https://my.telegram.org) بروید
2. شماره تلفن خود را با کد کشور وارد کنید:
   - مثال: `+989123456789`
3. روی **Next** کلیک کنید

### 1.2 - دریافت کد تایید

1. کد تایید به **تلگرام** شما ارسال می‌شود (نه SMS)
2. کد را در سایت وارد کنید
3. اگر رمز دو مرحله‌ای (2FA) دارید، آن را نیز وارد کنید

### 1.3 - ساخت Application

1. بعد از ورود، **API development tools** را انتخاب کنید
2. فرم را این‌طور پر کنید:
   - **App title:** `Yuan Rate Bot` (یا هر نام دلخواه)
   - **Short name:** `yuan_bot`
   - **Platform:** `Other`
   - **Description:** (خالی بگذارید یا توضیح بنویسید)
   - بقیه موارد را خالی بگذارید
3. روی **Create application** کلیک کنید

### 1.4 - کپی کردن اطلاعات

یک صفحه با اطلاعاتی مثل این نمایش داده می‌شود:

```
App api_id: 1234567
App api_hash: abcdef1234567890abcdef1234567890
```

⚠️ **این دو مقدار را کپی کنید و در Notepad یادداشت کنید!**

---

## 🤖 قدم ۲: ساخت ربات تلگرام (۵ دقیقه)

### 2.1 - رفتن به BotFather

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را بفرستید
3. نام ربات: `Yuan Rate Calculator` (یا هر نام دیگر)
4. Username ربات: `yuan_rate_calc_bot` (باید به `bot` ختم شود)

### 2.2 - دریافت توکن

BotFather توکنی مثل این می‌فرستد:

```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

⚠️ **این را هم کپی کنید و یادداشت کنید!**

---

## 👥 قدم ۳: ساخت گروه تلگرام (۳ دقیقه)

### 3.1 - ایجاد گروه

1. در تلگرام: **New Group**
2. نام: `Yuan Rate Updates` (یا هر نام دیگر)
3. ربات را به گروه اضافه کنید

### 3.2 - دریافت شناسه گروه

1. به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. یک پیام از گروه خود را forward کنید به این ربات
3. شناسه گروه را کپی کنید (مثل: `-1001234567890`)

⚠️ **یادداشت کنید! (با منفی شروع می‌شود)**

---

## 💻 قدم ۴: نصب و تست محلی (۱۰ دقیقه)

### 4.1 - نصب Python

اگر Python ندارید:
- Windows: از [python.org](https://www.python.org/downloads/) دانلود کنید
- حتماً "Add to PATH" را تیک بزنید

### 4.2 - نصب وابستگی‌ها

```powershell
cd "d:\Project\Telegram Tether"
pip install -r requirements.txt
```

### 4.3 - ایجاد فایل .env

```powershell
Copy-Item .env.example .env
notepad .env
```

فایل را با اطلاعات خود پر کنید:

```env
# توکن ربات (از BotFather)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789

# شناسه گروه
TARGET_GROUP_ID=-1001234567890

# کانال منبع
SOURCE_CHANNEL=tetherprice_toman

# اطلاعات Telethon (از my.telegram.org)
TELEGRAM_API_ID=1234567
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
TELEGRAM_PHONE=+989123456789

# منطقه زمانی
TIMEZONE=Asia/Tehran
```

⚠️ `PRIVATE_CHANNEL_ID` را **خالی بگذارید**

### 4.4 - تنظیم نرخ یوآن

ابتدا ربات را اجرا کنید:

```powershell
python bot.py
```

در تلگرام به ربات پیام دهید:
```
/setrate 7.12
```

سپس ربات را متوقف کنید: **Ctrl+C**

### 4.5 - اولین تست - خواندن خودکار! 🎉

```powershell
python auto_fetcher.py
```

**اولین بار:**
- یک کد تایید به تلگرام شما می‌آید
- کد را در PowerShell وارد کنید
- یک فایل `user_session.session` ایجاد می‌شود

**اگر موفق بود:**
```
INFO:__main__:🔄 شروع فرآیند خودکار...
INFO:__main__:وارد شدن با حساب کاربری و خواندن از @tetherprice_toman...
INFO:__main__:پیام دریافت شد از @tetherprice_toman
INFO:__main__:✅ قیمت تتر: 1,084,980 ریال
INFO:__main__:✅ نرخ مبنا: 15,240 تومان
INFO:__main__:✅ پیام با موفقیت ارسال شد!
```

و در گروه تلگرام پیام را خواهید دید! 🎉

✅ **عالی! قسمت محلی کار می‌کند!**

---

## 📤 قدم ۵: آماده‌سازی برای GitHub (۵ دقیقه)

### 5.1 - تبدیل Session به Base64

Session file را باید به GitHub بفرستیم (اما نه مستقیم، به صورت رمزشده):

```powershell
# در PowerShell اجرا کنید:
$bytes = [System.IO.File]::ReadAllBytes("user_session.session")
$base64 = [Convert]::ToBase64String($bytes)
$base64 | Out-File -FilePath "session_base64.txt"
notepad session_base64.txt
```

یا اگر خطا داد:

```powershell
python -c "import base64; data=open('user_session.session','rb').read(); print(base64.b64encode(data).decode())" > session_base64.txt
notepad session_base64.txt
```

محتوای فایل (یک رشته طولانی) را **کپی** کنید.

---

## 🔐 قدم ۶: تنظیم Secrets در GitHub (۱۰ دقیقه)

### 6.1 - ایجاد ریپازیتوری

1. وارد [GitHub](https://github.com) شوید
2. روی **+** > **New repository**
3. نام: `telegram-tether-bot`
4. Public یا Private (مهم نیست)
5. **Create repository**

### 6.2 - آپلود فایل‌ها

تمام فایل‌های پروژه را آپلود کنید:
- `bot.py`
- `auto_fetcher.py` ⭐ (جدید)
- `requirements.txt`
- `.gitignore`
- `.env.example`
- `README.md`
- پوشه `.github/workflows/`
- و بقیه فایل‌های `.md`

⚠️ **نکته:** فایل‌های `.env` و `user_session.session` را آپلود **نکنید** (`.gitignore` مانع می‌شود)

### 6.3 - اضافه کردن Secrets

GitHub > Settings > Secrets and variables > Actions > New repository secret

این **۶ Secret** را اضافه کنید:

| Name | Value | مثال |
|------|-------|------|
| `BOT_TOKEN` | توکن ربات | `123456:ABCdef...` |
| `TARGET_GROUP_ID` | شناسه گروه | `-1001234567890` |
| `TELEGRAM_API_ID` | API ID | `1234567` |
| `TELEGRAM_API_HASH` | API Hash | `abcdef123...` |
| `TELEGRAM_PHONE` | شماره تلفن | `+989123456789` |
| `TELETHON_SESSION` | Session (base64) | رشته طولانی که کپی کردید |

⚠️ نام‌ها باید **دقیقاً** همین باشد (با حروف بزرگ)

---

## ✅ قدم ۷: فعال‌سازی و تست (۵ دقیقه)

### 7.1 - فعال کردن Actions

1. GitHub > تب **Actions**
2. اگر "Workflows disabled" دیدید: **Enable workflows**

### 7.2 - اجرای دستی (تست)

1. Actions > **Update Tether Rate**
2. روی **Run workflow** کلیک کنید (دکمه سبز سمت راست)
3. **Run workflow** را بزنید
4. صبر کنید (۳۰-۶۰ ثانیه)

### 7.3 - بررسی نتیجه

1. روی workflow کلیک کنید
2. **update-rate** را باز کنید
3. هر step را باز کنید و لاگ‌ها را ببینید

**اگر موفق بود:**
- تیک سبز ✅
- در گروه تلگرام پیام را دیدید

**اگر خطا داد:**
- به بخش [عیب‌یابی](#عیب-یابی) بروید

---

## 🎉 تمام! ربات کاملاً خودکار است!

### از این به بعد چکار کنم؟

**هیچ کار خاصی! 🎊**

ربات:
- ✅ هر ساعت از ۱۱ صبح تا ۷ شب **خودکار** اجرا می‌شود
- ✅ **خودش** از کانال `@tetherprice_toman` می‌خواند
- ✅ محاسبه می‌کند و در گروه ارسال می‌کند
- ✅ بدون نیاز به forward دستی
- ✅ بدون نیاز به سیستم روشن

### فقط یک چیز: تنظیم نرخ یوآن

وقتی نرخ یوآن عوض شد، به ربات پیام دهید:

```
/setrate 7.15
```

**این هم می‌تواند خودکار شود** (اگر API نرخ یوآن داشته باشید - در آموزش پیشرفته توضیح داده شده)

---

## 🎯 مزایای این روش

| ویژگی | وضعیت |
|-------|--------|
| خواندن خودکار کانال | ✅ بدون forward دستی |
| اجرای خودکار | ✅ هر ساعت (۹ بار در روز) |
| نیاز به سرور | ❌ رایگان با GitHub Actions |
| نیاز به سیستم روشن | ❌ هیچ نیازی نیست |
| امنیت | ✅ Session رمزنگاری شده |
| قابلیت اعتماد | ✅ Telethon + Bot API |
| هزینه | 💰 **رایگان ۱۰۰٪** |

---

## 🐛 عیب‌یابی

### ❌ خطا: "Could not find the input entity"

**دلیل:** نام کانال اشتباه است

**راه‌حل:** 
- مطمئن شوید `SOURCE_CHANNEL=tetherprice_toman` (بدون @)
- نام کانال را چک کنید

### ❌ خطا: "You must be authorized"

**دلیل:** Session منقضی شده یا اشتباه است

**راه‌حل:**
1. Session را دوباره بسازید (قدم ۴ و ۵)
2. Secret `TELETHON_SESSION` را به‌روز کنید

### ❌ خطا: "Phone number is not registered"

**دلیل:** شماره تلفن اشتباه است

**راه‌حل:** 
- فرمت صحیح: `+989123456789` (با + و کد کشور)
- بدون فاصله یا خط تیره

### ❌ خطا: "The key 'TELETHON_SESSION' does not exist"

**دلیل:** Secret در GitHub اضافه نشده

**راه‌حل:**
- GitHub > Settings > Secrets > Actions
- همه ۶ Secret را چک کنید

### ❌ هیچ پیامی ارسال نمی‌شود

**چک‌لیست:**
- [ ] نرخ یوآن تنظیم شده؟ (`/setrate 7.12`)
- [ ] همه ۶ Secret در GitHub موجود است؟
- [ ] ربات عضو گروه است؟
- [ ] GitHub Actions فعال است؟
- [ ] لاگ‌های Actions چه می‌گوید؟

---

## 💡 نکات پیشرفته

### 🤖 خودکار کردن تنظیم نرخ یوآن

اگر API برای نرخ یوآن دارید، در `auto_fetcher.py` اضافه کنید:

```python
import requests

# دریافت خودکار نرخ یوآن
try:
    response = requests.get('https://api.yoursite.com/yuan-rate')
    yuan_rate = response.json()['rate']
    bot_instance.yuan_rate = yuan_rate
    bot_instance.save_data()
    logger.info(f"✅ نرخ یوآن خودکار دریافت شد: {yuan_rate}")
except Exception as e:
    logger.warning(f"⚠️ نمی‌توان نرخ یوآن را دریافت کرد: {e}")
```

### 📊 لاگ و مانیتورینگ

می‌توانید ربات را طوری تنظیم کنید که:
- وقتی خطا رخ داد به شما پیام بدهد
- گزارش روزانه بفرستد
- تاریخچه نرخ‌ها را ذخیره کند

### 🔔 اعلان‌های هوشمند

اضافه کردن شرط‌های هوشمند:
- اگر نرخ X درصد تغییر کرد اعلام کن
- اگر نرخ از حد خاصی گذشت هشدار بده

---

## 📚 فایل‌های مرتبط

- `SIMPLE_GUIDE.md` - راهنمای خیلی ساده
- `TUTORIAL.md` - آموزش کامل (روش دستی)
- `README.md` - مستندات کامل پروژه
- `ADVANCED.md` - تنظیمات پیشرفته

---

## 🎉 تبریک!

شما یک ربات **کاملاً خودکار** و **حرفه‌ای** ساختید که:

✅ بدون دخالت شما کار می‌کند  
✅ از کانال عمومی می‌خواند  
✅ هر ساعت به‌روزرسانی می‌کند  
✅ هیچ هزینه‌ای ندارد  
✅ قابل اعتماد و پایدار است  

**موفق و پرسود باشید! 🚀💰**

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:
1. لاگ‌های GitHub Actions را بررسی کنید
2. به ربات `/status` بدهید
3. فایل `data.json` را در ریپازیتوری چک کنید
4. در Issues گیت‌هاب سوال بپرسید
