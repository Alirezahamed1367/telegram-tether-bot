# 🚀 راهنمای سریع (Quick Start)

## ✅ چک‌لیست راه‌اندازی (۳۰ دقیقه)

### ۱. ساخت ربات (۵ دقیقه)
```
- [] وارد @BotFather شوید
- [] دستور /newbot را بزنید
- [] توکن ربات را کپی کنید
```

### ۲. گروه تلگرام (۳ دقیقه)
```
- [] گروه جدید بسازید
- [] ربات را به گروه اضافه کنید
- [] شناسه گروه را از @userinfobot دریافت کنید
```

### ۳. GitHub (۱۰ دقیقه)
```
- [] حساب GitHub بسازید (اگر ندارید)
- [] ریپازیتوری جدید ایجاد کنید
- [] فایل‌های پروژه را آپلود کنید
```

### ۴. تنظیم Secrets (۵ دقیقه)
```
- [] Settings > Secrets > Actions
- [] BOT_TOKEN را اضافه کنید
- [] TARGET_GROUP_ID را اضافه کنید
```

### ۵. فعال‌سازی (۲ دقیقه)
```
- [] تب Actions > Enable workflows
- [] Run workflow را بزنید (تست)
```

### ۶. تنظیم نرخ (۱ دقیقه)
```
- [] به ربات پیام بدهید: /setrate 7.12
- [] /status را بزنید برای بررسی
```

---

## 📋 ساختار فایل‌های پروژه

```
d:\Project\Telegram Tether\
│
├── bot.py                  # کد اصلی ربات ⭐
├── requirements.txt        # وابستگی‌های پایتون
├── .env.example           # نمونه فایل تنظیمات
├── .gitignore             # فایل‌های نادیده گرفته شده در Git
│
├── README.md              # مستندات اصلی 📖
├── TUTORIAL.md            # آموزش گام‌به‌گام 📚
├── ADVANCED.md            # راهنمای پیشرفته 🔧
│
├── test_bot.py            # اسکریپت تست 🧪
├── channel_reader.py      # خواندن کانال با Telethon (اختیاری)
├── data.json.example      # نمونه فایل داده
│
└── .github/
    └── workflows/
        └── update_rate.yml # GitHub Actions workflow ⚙️
```

---

## 💻 دستورات مفید

### نصب و اجرای محلی
```powershell
# نصب وابستگی‌ها
pip install -r requirements.txt

# کپی تنظیمات
Copy-Item .env.example .env
notepad .env  # ویرایش و اضافه کردن توکن

# اجرای ربات
python bot.py

# تست ربات
python test_bot.py
```

### دستورات ربات در تلگرام
```
/start              - راهنما
/setrate 7.12       - تنظیم نرخ یوآن
/getrate            - نمایش نرخ فعلی
/status             - وضعیت ربات
/update             - به‌روزرسانی دستی
```

---

## 🔥 نکات مهم

1. **هر روز قبل از ۱۱ صبح:** نرخ یوآن را با `/setrate` تنظیم کنید
2. **GitHub Actions:** هر ساعت از ۱۱ صبح تا ۷ شب اجرا می‌شود
3. **مشکل خواندن کانال:** ربات نمی‌تواند مستقیماً از `@tetherprice_toman` بخواند
   - راه‌حل: یک کانال میانی بسازید و ربات را ادمین کنید
   - یا از `channel_reader.py` با Telethon استفاده کنید

4. **شرط نرخ:** اگر نرخ جدید < نرخ قبلی → از نرخ قبلی استفاده می‌شود

---

## 🎯 نمونه محاسبه

```
ورودی:
  • قیمت فروش تتر: 1,084,980 ریال
  • نرخ یوآن: 7.12

محاسبه:
  1. تبدیل به تومان: 1,084,980 ÷ 10 = 108,498
  2. تقسیم بر یوآن: 108,498 ÷ 7.12 = 15,238.48
  3. رند به بالا: 15,240

خروجی:
  • نرخ مبنا: 15,240 تومان
  • تا 5 هزار: 15,300 تومان
  • تا 10 هزار: 15,290 تومان
  • بالای 10 هزار: 15,280 تومان
```

---

## 🐛 عیب‌یابی سریع

| مشکل | راه‌حل |
|------|--------|
| پیام ارسال نمی‌شود | ربات عضو گروه است? Secrets تنظیم شده? |
| نرخ محاسبه نمی‌شود | نرخ یوآن با `/setrate` تنظیم کنید |
| GitHub Actions کار نمی‌کند | Actions را enable کنید |
| خطای کانال | از کانال میانی استفاده کنید |

---

## 📚 مستندات کامل

- **راهنمای کامل:** `README.md`
- **آموزش قدم‌به‌قدم:** `TUTORIAL.md`
- **تنظیمات پیشرفته:** `ADVANCED.md`

---

## ✨ ویژگی‌های کلیدی

✅ بدون نیاز به سرور (GitHub Actions)  
✅ اجرای خودکار هر ساعت  
✅ محاسبه هوشمند با رند کردن  
✅ شرط حفظ نرخ در کاهش  
✅ ذخیره خودکار داده‌ها  
✅ کنترل کامل از طریق دستورات  

---

**موفق باشید! 🎉**

برای شروع: `TUTORIAL.md` را باز کنید 👉
